using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class InventoryUI : MonoBehaviour
{
    private List<SlotUI> slotsList = new List<SlotUI>();
    
     
    
    public void UpdateUI(LinkedList<ItemSO> items)
    {
        foreach (var VARIABLE in slotsList)
        {
            
        }
    }

}
